name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Extract npm version
        id: extract_npm_version
        run: |
          VERSION=$(node -p -e "require('./package.json').version")
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build Docker image with version tag
        run: docker build -t ghcr.io/${{ github.repository }}/your-app:${{ env.VERSION }} .

      - name: Tag Docker image as latest
        run: docker tag ghcr.io/${{ github.repository }}/your-app:${{ env.VERSION }} ghcr.io/${{ github.repository }}/your-app:latest

      - name: Push Docker image with version tag
        run: docker push ghcr.io/${{ github.repository }}/your-app:${{ env.VERSION }}

      - name: Push Docker image with latest tag
        run: docker push ghcr.io/${{ github.repository }}/your-app:latest

      - name: Output Docker image URL
        run: |
          echo "Docker Image URL: ghcr.io/${{ github.repository }}/your-app:${{ env.VERSION }}"
          echo "Docker Image URL: ghcr.io/${{ github.repository }}/your-app:latest"
